version: "3.8"

services:
  mcp_stdio:
    build: .
    stdin_open: true
    tty: false
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_API_BASE=${OPENAI_API_BASE}
    restart: unless-stopped
